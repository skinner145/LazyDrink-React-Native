class Product {
    constructor(id, ownerId, title, type, price){
        this.id = id;
        this.ownerId = ownerId;
        this.title = title;
        this.type = type;
        this.price = price;
    }
}

export default Product;