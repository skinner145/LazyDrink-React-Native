class CartItem {
    constructor(quantity, productTitle, price, sum){
        this.quantity = quantity;
        this.productTitle = productTitle;
        this.price = price
        this.sum = sum
    }
}

export default CartItem;
